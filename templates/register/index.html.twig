{% extends 'base.html.twig' %}

{% block title %}S'inscrire{% endblock %}

{% block body %}
    <h1 class="text-dark">S'inscrire</h1>

    <hr class="text-dark" style="opacity: unset;">

    <section>
        {{ form_start(register_form) }}
            {{ form_widget(register_form) }}

            <button class="btn btn-primary col-2 offset-10 {% if app.user %}disabled{% endif %}">S'inscrire</button>
        {{ form_end(register_form) }}
    </section>
{% endblock %}
