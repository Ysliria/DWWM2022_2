{% extends 'base.html.twig' %}

{% block title %}{{ filiere.name }}{% endblock %}

{% block body %}
    <h1 class="text-dark">{{ filiere.name }}</h1>

    <hr class="text-dark" style="opacity: unset;">

    <div class="mb-3 row">
        <div class="col-2">
            <a href="{{ path('filiere_update', {'filiere': filiere.id}) }}" class="btn btn-warning">
                <i class="fa fa-pencil"></i> Modifier
            </a>
        </div>

        {% include 'filiere/_delete_filiere.html.twig' %}
    </div>

    <section class="row">
        @todo : liste des formations par filières
    </section>
{% endblock %}

